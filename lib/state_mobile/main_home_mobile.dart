import 'package:flutter/material.dart';

class MainHomeMobile extends StatelessWidget {
  const MainHomeMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text('This is Mobile')),
    );
  }
}