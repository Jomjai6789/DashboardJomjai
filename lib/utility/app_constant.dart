import 'package:flutter/material.dart';

class AppConstant {
  static String pageMainHomeMobile = '/mainHomeMobile';
  static String pageDashBoardWebsite = '/dashBoardWebsite';

  // Colors used in this app
  static const primaryColor = Color.fromRGBO(17, 159, 250, 1);
  static const secondaryColor = Colors.white;
  static const bgColor = Color.fromRGBO(247, 251, 254, 1);
  static const textColor = Colors.black;
  static const lightTextColor = Colors.black26;
  static const transparent = Colors.transparent;

  static const grey = Color.fromRGBO(148, 170, 220, 1);
  static const purple = Color.fromRGBO(165, 80, 179, 1);
  static const orange = Color.fromRGBO(251, 137, 13, 1);
  static const green = Color.fromRGBO(51, 173, 127, 1);
  static const red = Colors.red;

// Default App Padding
  static const appPadding = 16.0;
}
