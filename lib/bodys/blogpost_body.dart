import 'package:flutter/material.dart';

class BlogPostBody extends StatelessWidget {
  const BlogPostBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('This is Blog Post');
  }
}