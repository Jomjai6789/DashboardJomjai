import 'package:flutter/material.dart';

class DashBoardWebsite extends StatelessWidget {
  const DashBoardWebsite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       body: SafeArea(child: Text('This is Website')),
    );
  }
}