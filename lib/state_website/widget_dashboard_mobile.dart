import 'package:flutter/material.dart';

class WidgetDashboardMobile extends StatelessWidget {
  const WidgetDashboardMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Show Website on mobile');
  }
}